Config = {}

Config.Framework = 'qbcore' --The Framework from the server. Valid options are qb-core or esx
Config.DBUpdateInterval = 300000 --in milliseconds

Config.LockVehiclesAtSpawn = true --If set to true, the script will lock vehicles when spawned

Config.AutoCreateTableInDB = true --If set to true, the script will create the database if it does not already exist